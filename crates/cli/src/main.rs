//! `sendgrid` — the agent-facing CLI (and `sendgrid mcp` server host).
//! P2/P3 build the dynamic command tree + MCP subcommand from the IR.

fn main() {
    eprintln!("sendgrid CLI — not yet implemented");
    std::process::exit(64);
}
